import React from 'react';
import './JPProjects.css';
import { Typography, List, ListItem } from '@material-ui/core';

function JPProjects(){
    return (
    <div>
    <Typography variant='h2' className="Section-title">Github</Typography>
    </div>
  );
};

JPProjects.propTypes = {};

JPProjects.defaultProps = {};

export default JPProjects;
